<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="right"><%= link_to "Create New User", new_user_path %></div>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>